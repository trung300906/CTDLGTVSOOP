#include <iostream>

using namespace std;

int main() {
    int a, b;
    
    // Input
    cin >> a;
    cin >> b;

    // Compare and output
    if (a == b) {
        cout << 1 << endl;
    } else {
        cout << 0 << endl;
    }

    return 0;
}
