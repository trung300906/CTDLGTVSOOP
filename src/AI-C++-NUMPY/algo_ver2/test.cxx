#if 0
#include "include/numpy.hpp"
int main()
{
    numpy::ndarray<double> A({3, 3});
    std::cin >> A;
    numpy::ndarray<double> B({3, 3});
    std::cin >> B;
    numpy::ndarray<double> C = (A + B) * 2;
    std::cout << C * 2;
}
#endif
#if 0
#include <bits/stdc++.h>
using namespace std;
int main()
{
    cout << pow(0, 0);
}
#endif
