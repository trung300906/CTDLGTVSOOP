#include <bits/stdc++.h>
using namespace std;

auto process(vector<vector<char>> mirror){
    for(auto &i: mirror){
        
    }
}
int main()
{
    int R, L;
    cin >> R >> L;
    vector<vector<char>> mirror(R, vector<char>(L));
    for (auto &i : mirror)
    {
        for (auto &j : i)
        {
            cin >> j;
        }
    }
    process(mirror);
}