#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v; // vector with not elements
    // push_back vector
    for (int i = 0; i < 10; i++)
    {
        v.push_back(i);
    }
}